# Public (browser-safe) values. Real values are provided by Lovable Cloud.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Server-side only (Edge Function secrets — never prefix with VITE_):
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, LOVABLE_API_KEY
# Optional: ALLOWED_ORIGINS (comma-separated extra CORS origins)
